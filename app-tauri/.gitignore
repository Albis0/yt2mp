node_modules/
dist/
.DS_Store

# Rust build output
src-tauri/target/

# Bundled binaries — too large for the repo, fetched with
# `bun run fetch:binaries`. See README.
src-tauri/resources/

# Groq API keys for AI search — never committed (GitHub push protection
# blocks real keys anyway, and a checked-in key is a checked-in key).
src-tauri/.env
.env.local

# Generated by `tauri icon`; the source is src/assets/icon.png
src-tauri/gen/
